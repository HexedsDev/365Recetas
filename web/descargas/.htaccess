# Bloquea el acceso directo al PDF por URL.
# El archivo solo se entrega a través de api/descargar.php (con token de pago válido).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
